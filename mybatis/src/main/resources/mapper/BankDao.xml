<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.mybatis.dao.BankDao">

    <resultMap id="BaseResultMap" type="Bank">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="version" column="version"/>
        <result property="gmtCreate" column="gmt_create"/>
        <result property="gmtModified" column="gmt_modified"/>
        <result property="delete" column="is_delete"/>
    </resultMap>

    <insert id="add" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_bank (name)
        VALUES (#{name})
    </insert>

    <select id="get" resultMap="BaseResultMap">
        SELECT id, name, version, gmt_create, gmt_modified, is_delete
        FROM sys_bank
        WHERE id = #{id}
          AND is_delete = 0
    </select>

    <select id="getAll" resultMap="BaseResultMap">
        SELECT id, name, version, gmt_create, gmt_modified, is_delete
        FROM sys_bank
        WHERE is_delete = 0
        ORDER BY gmt_create
    </select>

    <select id="getPage" resultMap="BaseResultMap">
        SELECT id, name, version, gmt_create, gmt_modified, is_delete
        FROM sys_bank
        WHERE is_delete = 0
        ORDER BY gmt_create
        limit #{startIndex}, #{endIndex}
    </select>

    <update id="update">
        UPDATE sys_bank
        <set>
            <if test="name!=null">
                name=#{name},version=version+1
            </if>
        </set>
        WHERE id=#{id} AND version=#{version} AND is_delete=0
    </update>

    <update id="delete">
        update sys_bank
        set is_delete=1,
            version=version + 1
        WHERE id = #{id}
          AND version = #{version}
          AND is_delete = 0
    </update>

</mapper>